# Factory Playground

A tiny zero-dependency static site. It exists to be improved by the
[turbodiff](https://turbodiff.dev) software factory: features are planned,
generated, reviewed, auto-fixed, and visually verified by agents.

Run: `npm start` (serves on port 3000).
